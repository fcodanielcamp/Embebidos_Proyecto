services:
  mariadb:
    image: mariadb:latest
    container_name: mariadb_datos
    env_file: ../../.env
    ports:
      - "3306:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql
    restart: always

  # Aquí podrías agregar Grafana en el futuro para analítica visual